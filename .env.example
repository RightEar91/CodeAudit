# MySQL 数据库
MYSQL_ROOT_PASSWORD=codeaudit123

# 前端端口（默认 80，冲突时改）
FRONTEND_PORT=80

# AI 提供商：ollama（本地）/ openai（云端）
CODEAUDIT_AI_PROVIDER=ollama

# Ollama 地址（Docker 容器访问宿主机）
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=qwen3:8b

# OpenAI 兼容 API（使用云端模型时填写）
# OPENAI_API_KEY=sk-xxxxx
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_MODEL=gpt-4o

# GitHub（可选，使用 GitHub 仓库功能时填写）
# GITHUB_TOKEN=ghp_xxxxx
